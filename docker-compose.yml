version: "3.9"

services:
  auth:
    container_name: auth_service
    build: ./auth
    ports: 
      - "8500:8083"
    restart: always
  chat:
    container_name: chat_service
    build: ./chat
    ports:
      - "8501:8085" 
    restart: always
      